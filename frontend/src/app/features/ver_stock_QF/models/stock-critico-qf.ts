export interface StockCriticoQf {
    id: string;
  name: string;
  generic: string;
  details: string; // no viene del backend
  category: string; // no viene del backend
  status: string;
  units: number;
  manufacturer: string;
  dosage: string;
  imageUrl: string;
}
